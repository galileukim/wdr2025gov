% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_shapefile_zip.R
\name{get_shapefile_from_zip_url}
\alias{get_shapefile_from_zip_url}
\title{Download and extract a shapefile from a zipped URL}
\usage{
get_shapefile_from_zip_url(zip_url)
}
\arguments{
\item{zip_url}{A character string. The URL to a \code{.zip} file containing a shapefile
(must include \code{.shp}, \code{.shx}, \code{.dbf}, and optionally \code{.prj}).}
}
\value{
A character string: the full path to the extracted \code{.shp} file.
This path can be passed directly to \code{sf::st_read()}.
}
\description{
This function downloads a ZIP file from a given URL, extracts it to a temporary
directory, locates the first \code{.shp} file, and returns its full path. It is designed
to be used with \code{sf::st_read()} to load shapefiles that cannot be read directly
from the URL (e.g., IBGE's geoftp).
}
\examples{
\dontrun{
library(sf)
url <- "https://geoftp.ibge.gov.br/organizacao_do_territorio/malhas_territoriais/malhas_municipais/municipio_2024/Brasil/BR_Municipios_2024.zip"
shp_path <- get_shapefile_from_zip_url(url)
br_muni <- st_read(shp_path, options = "ENCODING=WINDOWS-1252")
}

}
